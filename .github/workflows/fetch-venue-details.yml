name: Fetch Venue Details (temp)
on:
  workflow_dispatch:
jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Lenin statue venue
        run: |
          curl -s "https://api.foursquare.com/v2/venues/62b1b6058e0dc66cf294ed6b?oauth_token=${{ secrets.FOURSQUARE_TOKEN }}&v=20231010"
      - name: Fetch Ziarul Comunistul venue
        run: |
          curl -s "https://api.foursquare.com/v2/venues/4f1986efe4b012690c8819de?oauth_token=${{ secrets.FOURSQUARE_TOKEN }}&v=20231010"
